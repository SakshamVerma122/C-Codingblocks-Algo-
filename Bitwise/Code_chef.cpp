#include<bits/stdc++.h>
using namespace std;

int main()
{
	int T,N,D,H;
	
	cin>>T;
	
	while(T>0)
	{
		cin>>N>>D>>H;
		vector<int> V
		
		int value;
		for (int i = 0; i < N; ++i)
		{
			cin>>value;
			v.push_back(value);
		}

		for(value:v)
		{
			if
		}
		--T;
	}
}