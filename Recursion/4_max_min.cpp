#include<bits/stdc++.h>

vector<pair<int ,int>> min_max(vector<int> arr,int start, int end)
{
    if(start == end)
    {
        return vector<pair<int ,int>> v.push_back( make_pair(arr[start],arr1[end]) );
    }
}